{
    "compilerOptions": {
      "target": "ES2018",               // ou o target que você preferir
      "module": "commonjs",          // Usando CommonJS (padrão para Node)
      "types": ["node"]              // Adicionando os tipos do Node
    }
  }
  